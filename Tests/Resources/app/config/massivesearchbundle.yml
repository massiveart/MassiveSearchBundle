framework:
    router:
        resource: %kernel.root_dir%/config/routing.yml

massive_search:

    # use the test search adapter
    adapter: test

    # the index localization strategy appends the locale to the index name
    localization_strategy: index
